export interface Readings {
  name: string;
  temperature: number;
  humidity: number;
  dateTime: string;
  rainFallVolume: number;
}
export interface Weather {
  description: string;
  icon: string;
}
